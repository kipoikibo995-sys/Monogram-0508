sed -i '/{view === .dashboard. && (/i \          {view === "settings" && (\n            <motion.div\n              key="settings"\n              initial={{ opacity: 0, y: 10 }}\n              animate={{ opacity: 1, y: 0 }}\n              exit={{ opacity: 0, y: -10 }}\n              transition={{ duration: 0.2 }}\n              className="flex-1 flex overflow-hidden"\n            >\n              <SettingsView user={user} onLogout={() => signOut(auth)} />\n            </motion.div>\n          )}' src/App.tsx
