sed -i '/<div className="mt-auto pt-4 pb-1">/i \            <button \n              onClick={() => setView("settings")}\n              className={`relative z-0 flex items-center gap-2 px-3 py-2 mt-auto rounded-md transition-colors text-sm font-semibold ${view === "settings" ? "text-neutral-900" : "text-neutral-500 hover:bg-neutral-50 hover:text-neutral-700"}`}\n            >\n              {view === "settings" && (\n                <motion.div\n                  layoutId="activeView"\n                  className="absolute inset-0 bg-neutral-100 rounded-md -z-10"\n                  transition={{ type: "spring", bounce: 0.2, duration: 0.6 }}\n                />\n              )}\n              <Settings size={16} />\n              Settings\n            </button>' src/App.tsx
